name: tms-infra
description: AWS TypeScript Pulumi Program to deploy serverless components like lambda, dynamodb, cognito
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: aws-typescript
